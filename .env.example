# Copy to `.env.local` and adjust as needed. Defaults assume the api-gateway is
# on http://localhost:8080 and the Vite dev server is on http://localhost:3000
# (which is already in the gateway's CORS allow-list).
VITE_GATEWAY_URL=http://localhost:8080
